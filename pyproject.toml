[tool.poetry]
name = "cac-benchmark-toolkit"
version = "2.0.0"
description = "Reproducible benchmark pipeline for the Convolution Analytics Cube (CAC)"
package-mode = false

[tool.poetry.dependencies]
python = ">=3.11"
duckdb = ">=1.1.0"
pyroaring = ">=1.1.0"
pandas = ">=2.0.0"
pyarrow = ">=15.0.0"
pyyaml = ">=6.0"
psycopg2-binary = ">=2.9.0"
boto3 = ">=1.34.0"
requests = ">=2.31.0"
psutil = ">=5.9.0"
